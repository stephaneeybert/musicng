// dateOfPublication: new FormControl(new Date()), TODO add a field

  <mat-dialog-content>
    <mat-form-field>
      <input formControlName="dateOfPublication" matInput [matDatepicker]="picker" placeholder="TODO Choose a date of publication"
        readonly (click)="picker.open()">
      <mat-datepicker-toggle matSuffix [for]="picker"></mat-datepicker-toggle>
      <mat-datepicker #picker></mat-datepicker>
    </mat-form-field>
  </mat-dialog-content>

Avoir une sortie MIDI
Export de fichier MIDI
Live streaming MIDI

La deuxième portée de la symphonie a un son moins fort que la première

Make sure all constants are typed.

All methods should have a return type, possibly void.

Have an assistant to showcase a tour of the app for newbies

Do a slider
https://material.angular.io/guide/getting-started
<mat-slider min="1" max="100" step="1" value="1"></mat-slider>

How come there is a common ui and utils service ?

Have an installable to home screen app

The statement
Tone.Transport.bpm.rampTo
does not accept the tempo of 128
but the 120 is fine
Change the measure tempo to be a number of beats

Could I catch this vexflow no svg context exception ?
RuntimeError {code: "NoCanvasContext", message: "Can't draw without a canvas context."}

In all add... methods return the added object, like for the addTrack method

Ask Norbert:
